node_modules/
dist/
*.log
.DS_Store

# Local git worktrees used for isolated feature work — never committed
.worktrees/

# Netlify CLI state — not user code
.netlify/
# Written by the edge-function bundler when `netlify build` runs locally.
web/deno.lock

# secrets — never commit
.env
.env.*
*.pem
*.key
*.keystore
secrets.json
